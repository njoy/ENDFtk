add_executable( section3.test 3.test.cpp )
set( test_flags ${ENDFtk_compiler_flags} )
separate_arguments( test_flags_list UNIX_COMMAND "${test_flags}" )
foreach( flag IN LISTS test_flags_list )
    target_compile_options( section3.test PUBLIC ${flag} )
endforeach( flag )
set_target_properties( section3.test PROPERTIES LINK_FLAGS "${test_flags}" )
target_link_libraries( section3.test PUBLIC ENDFtk )
add_test( NAME section3 COMMAND section3.test ) 
