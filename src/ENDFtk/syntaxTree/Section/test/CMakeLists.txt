add_executable( ENDFtk.syntaxTree.Section.test Section.test.cpp )
set( test_flags ${ENDFtk_compiler_flags} )
separate_arguments( test_flags_list UNIX_COMMAND "${test_flags}" )
foreach( flag IN LISTS test_flags_list )
    target_compile_options( ENDFtk.syntaxTree.Section.test PUBLIC ${flag} )
endforeach( flag )
set_target_properties( ENDFtk.syntaxTree.Section.test PROPERTIES LINK_FLAGS "${test_flags}" )
target_link_libraries( ENDFtk.syntaxTree.Section.test PUBLIC ENDFtk )
add_test( NAME ENDFtk.syntaxTree.Section COMMAND ENDFtk.syntaxTree.Section.test ) 
